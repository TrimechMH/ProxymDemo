/**
 * Created by TrimecheMH on 2/26/2016.
 */
/**
 *
 * @type {demo|{}}
 */
demo = (typeof(demo) !== "undefined") ? demo : {};
/**
 *
 * @type {{}}
 */
demo.contants ={
    // Get the path of modules
    BASE_MODULES_PATH  : "public/js/app/modules/",
    //  get the path of partial folder
    RELATIVE_PARTIAL_MODULE_PATH: "partials/",
    // List of the module name.
    modules : {
         AUTHENTICATION_MODULE_NAME : "authentication"
    }
};
